package dev.kaffah

import io.flutter.embedding.android.FlutterActivity

/**
 * Optional convenience base Activity. Apps with an existing base class should
 * use [PipHelper.attach] directly instead.
 */
open class LiveKitPipActivity : FlutterActivity()
