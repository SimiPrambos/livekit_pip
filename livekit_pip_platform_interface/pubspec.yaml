name: livekit_pip_platform_interface
description: Platform interface for the livekit_pip plugin — defines the shared contract for Android and iOS implementations.
version: 0.2.0
homepage: https://github.com/SimiPrambos/livekit_pip
repository: https://github.com/SimiPrambos/livekit_pip/tree/main/livekit_pip_platform_interface

environment:
  sdk: ^3.11.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.2.0
